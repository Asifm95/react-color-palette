import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  root: {
    display: 'flex',
    justifyContent: 'flex-end',
    height: '4vh',
    alignItems: 'center',
    fontSize: '1.2rem',
    fontWeight: 500,
  },
  emoji: {
    fontSize: '1rem',
    margin: '1rem',
  },
});
