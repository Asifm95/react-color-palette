import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  palette: {
    height: '100vh',
    overflow: 'hidden',
  },

  paletteColors: {
    height: '90%',
    display: 'flex',
    flexWrap: 'wrap',
  },
});
